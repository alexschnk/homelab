tcp:
 
  routers:
    sftp:
      entryPoints:
        - "sftp"
      rule: "HostSNI(`*`)"      
      service: sftp

  services:
    sftp:
      loadBalancer:
        servers:
          - address: "192.168.31.201:22" 
       
