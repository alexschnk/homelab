services:
  rclone:
    image: rclone/rclone
    container_name: rclone
    command: rcd --rc-addr=0.0.0.0:5572 --rc-no-auth --rc-web-gui --rc-allow-origin='https://domain.ru' # --rc-allow-origin='*'
 #   ports:
 #     - 5572:5572
    volumes:
      - ./config:/config/rclone
      - ./data:/data
    networks:
      proxy:
    security_opt: 
      - no-new-privileges
#    tty: true
networks:
  proxy:
     name: proxy # Specifies the external network to connect to
     external: true
