# Configuration file for Synapse.
#
# This is a YAML file: see [1] for a quick introduction. Note in particular
# that *indentation is important*: all the elements of a list or dictionary
# should have the same indentation.
#
# [1] https://docs.ansible.com/ansible/latest/reference_appendices/YAMLSyntax.html
#
# For more information on how to configure Synapse, including a complete accounting of
# each option, go to docs/usage/configuration/config_documentation.md or
# https://element-hq.github.io/synapse/latest/usage/configuration/config_documentation.html
server_name: "dt.moyservak.ru"
pid_file: /data/homeserver.pid
listeners:
  - port: 8008
    resources:
    - compress: false
      names:
      - client
      - federation
    tls: false
    type: http
    x_forwarded: true
    bind_addresses: ['0.0.0.0']

database:
  name: psycopg2
  args:
    user: synapse
    password:  password123!
    database: synapse
    host: postgres
    cp_min: 5
    cp_max: 10

log_config: "/data/dt.moyservak.ru.log.config"
media_store_path: /data/media_store
registration_shared_secret: "l@DDN1;v1@+F9Cq;Y=JqZXR8KxnlOt9El#R_f2T^;;qENMZJXI"
report_stats: false
macaroon_secret_key: "#D_A6fmx8sup;Qs,=6UtY-BMZFovzoX.~z^0aTWa^mn^BEV#:k"
form_secret: "Fnjo9uIbiZjmDGaRM3qpW*EfSutgKS~3wb5dvsZdb~.b_X=wa~"
signing_key_path: "/data/dt.moyservak.ru.signing.key"
trusted_key_servers:
  - server_name: "matrix.org"

federation_domain_whitelist: []
enable_registration: true
enable_registration_without_verification: true
enable_registration_captcha: false
password_config:
   enabled: true
auth:
  type: "password"

# vim:ft=yaml
